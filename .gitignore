# Private planning / strategy — keep OUT of the public repo
PROJECT.md

# Old prototype + local-only files
prototype.html
site/

# Capacitor / build artifacts (generated — never committed)
node_modules/
www/

# OS / editor / temp junk
.DS_Store
Thumbs.db
*.log
_test_*.mp3
resp.json
design-previews/
