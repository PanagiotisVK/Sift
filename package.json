{
  "name": "sift",
  "private": true,
  "version": "1.0.0",
  "description": "Sift — swipe-to-discover music app",
  "scripts": {
    "www": "node scripts/build-www.js",
    "sync": "npm run www && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
